#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
export PYTHONPATH="${REPO_ROOT}/strategic_alpha/src${PYTHONPATH:+:${PYTHONPATH}}"
python3 -m strategic_alpha.cli "$@"
